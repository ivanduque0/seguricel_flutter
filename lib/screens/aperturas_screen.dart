import 'package:flutter/material.dart';

class AperturasScreen extends StatelessWidget {
  const AperturasScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("aperturasxd"),
    );
  }
}